    
    // l(newDate);
    // let dias = moment().diff(pasado, 'days');
    // if(dias >= 30){
    //     l(pasado.format('lll'))
    // }else {
    //     l(dias + ' days ago')
    // }

       // data();
    const copyData = () => {
        let innerCode = document.querySelector('#innerCode');
        let copyDataCode = innerCode.value;
        console.log(copyDataCode)     
    }

